# Bounded v5 digital canary — 2026-09-08

State: prospectively scoped and authorized under the user's standing technical
authority; no execution is claimed by this document. The earlier v4 batch and
its eight timeouts remain consumed and immutable.

The [synthetic workload qualification](perceptual-degradation-global-workload-result-20260908.md)
passed its exact-equivalence, replay and 120-second margin gates. This permits
a new bounded test, not an assumption that real-source throughput will pass.
The [machine-readable proposal](../../benchmarks/perceptual-degradation-v1/digital-v5-canary-proposal-20260908.json)
and [batch authorization](../../benchmarks/perceptual-degradation-v1/digital-v5-canary-authorization-20260908.json)
bind the standing approval, successful audited qualification, frozen v5 code,
included native source, compiler, unchanged codec closure, runner and auditors.

## Fixed scope

Select the largest declared full-length PCM reference from the same hash-bound
16-member retained development inventory; break a frame-count tie by ascending
opaque delivery identity. Selection reads metadata, not waveform descriptors.
Open only that selected reference. It is the already consumed development
reference, not fresh validation evidence or a new source/partition assignment.

Run two ordered six-case replays: identity, float32 adapter, MP3 128 kb/s,
AAC 128 kb/s, Opus 128 kb/s CBR with 20 ms frames, and Vorbis q6. The exact
predecessor encoder/probe/decoder argv are unchanged. This is twelve comparisons
and at most eight encode/decode pairs with one worker. No waveform selection,
substitution, playback, capture, provider-processed material or provider scores.

V5 changes only the implementation of global refinement correlation. All
scientific thresholds, lag/sample inventories, normalization semantics,
duration limits, local searches, gain/delay/drift policy and input samples
remain unchanged. The existing 180-second case, 60-second command and
1,200-second replay-start limits remain. A negative result is retained, not
repaired by increasing the deadline or retrying a consumed batch.

Keep a 15 GiB reserve plus 384 MiB scratch allowance on the output volume;
also check the external workspace reserve. Create a fresh private run directory
and an exclusive parent launch marker. The marker is consumed even if later
preflight fails; never overwrite it or silently relaunch elsewhere.
Preserve each timeout, failure and unstarted slot. Resource/replay stops carry
into the second replay. Temporary adapter, encoded and decoded files are
removed by the existing per-case temporary-directory context; retain no audio.

## Evidence and success criteria

Each private case preserves the full alignment record, input/codec hashes,
packet geometry, local-native accounting and separate global-native accounting.
The global inventory includes search counts, short overlaps, fallback counts
and a digest/count of canonical overlap/stride geometry plus sampled-pair work.
It omits the potentially large geometry-row list prospectively, not numerical
alignment evidence. Partial global work remains visible on timeout.

Compare canonical private replay bytes after removing exactly each case's
five-field timing object; no other field is excluded. Computational completion
means all twelve comparisons observed, repeatable evidence and four passing
replay edges. The stronger primary technical gate also requires both controls
to be technically supported in both rounds and zero local/global fallback.
Codec support/abstention is reported separately, not required to fabricate a
pass and never interpreted as audibility or quality.

The independent auditor reconstructs aggregates, verifies the exclusive launch
marker, exact selected source and saved artifact inventory, and separately
checks completed local/global candidate totals. The runner's validator is an
additional consistency check, not the independent aggregate implementation.
Retain five private JSON files plus the hash-bound native library; no derived
audio. Public projections contain no source identity, paths or audio hashes.

## Execution gate and disposition

Before any private metadata/media access, require a clean committed checkout,
isolated Python, the new exact authorization, unchanged transitive bindings,
full local CI-equivalent tests, deterministic SBOM and staged/privacy audit,
then a successful push CI run for the exact execution head. Recheck native
tool/compiler bindings and source integrity at replay edges. Audit saved
artifacts independently before publication; validate the final public projection.

The standing authority permits later separately frozen technical attempts
without asking again for each shot. It does not reopen this or any predecessor
after consumption. Metrics, listening, human collection, source-trait promotion,
training, sealed evidence, outreach/spending, merge/release and public verdicts
remain closed. No perceptual output is populated. The broader objective
remains incomplete regardless of this engineering result.

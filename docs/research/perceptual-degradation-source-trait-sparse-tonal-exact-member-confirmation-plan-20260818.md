# Perceptual-degradation sparse/tonal exact-member confirmation plan - 2026-08-18

**Status:** frozen before either selected audio member is opened. The runner has
been replayed only on synthetic integer-PCM fixtures.

This checkpoint binds the prior metadata selection and the prior score-blind
descriptor implementation. A future live run may acquire only Freesound sound
856645 as its provider-original WAV and may project only the selected TinySOL
oboe member into memory from the already retained archive. No other TinySOL
member may be extracted or measured.

The runner accepts only the predeclared 44.1 kHz integer-PCM containers, measures
each channel independently, and requires every supported channel to agree with
the candidate's predeclared contrast. It performs two fresh one-worker replays,
requires byte-identical private reports, attaches source attribution, preserves
at least 15 GiB free disk, and retains no decoded or derived PCM. The public
projection excludes private paths and encoded or PCM hashes.

An abstention or class mismatch is a valid negative result. It is recorded
without changing the descriptor thresholds, substituting a candidate, assigning
a source trait, or allocating a source manifest inside this checkpoint.

The plan does not authorize codec generation, perceptual metrics, playback,
listener collection, provider scores or processed conditions, sealed evidence,
no-reference training, source-trait assignment, manifest allocation, validation
claims, or a public verdict.

Artifacts:

- [frozen plan](../../benchmarks/perceptual-degradation-v1/source-trait-sparse-tonal-exact-member-confirmation-plan.json)
- [frozen runner](../../scripts/perceptual_degradation_source_trait_sparse_tonal_exact_member_confirmation.py)

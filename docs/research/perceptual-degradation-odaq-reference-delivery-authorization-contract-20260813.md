# ODAQ clean-reference delivery authorization contract - 2026-08-13

**State:** execution contract frozen; responsible-human authorization pending;
retained reference reads and projections remain forbidden

## Outcome

The exact acquisition, attribution, deterministic projection, and completed
48 kHz physical-playback qualification are now joined by one machine-validated
successor-authorization contract. This closes ambiguity about what a future
authorization may enable without treating the existing acquisition choice or
playback observations as permission to convert retained audio.

No retained reference was read, hashed, converted, played, or otherwise
accessed while preparing this contract. No processed ODAQ condition, published
score, metric, degradation rating, listener response, or sealed evidence was
opened. The projection implementation still has no live-corpus command.

## Evidence bound by the future authorization

A valid successor authorization must bind, by repository-relative path and
SHA-256:

- the completed 16-reference acquisition result and its private inventory
  digest;
- the synthetic-only projection preparation and exact conversion code;
- the attribution audit, including all 16 ready attribution notices;
- the completed 48 kHz playback-chain qualification; and
- this execution plan and authorization schema.

The historical projection preparation correctly records that playback was not
qualified at the time it was frozen. The later path-free qualification result
supersedes only that gate: it does not modify the historical artifact and does
not authorize conversion, stimuli, metrics, or ratings.

## Narrow positive authority

Only a separately committed responsible-human record matching the frozen JSON
schema may enable:

- reading the exact 16 retained clean references already acquired;
- applying only the frozen canonical integer-PCM delivery projection;
- creating private delivery copies outside the repository;
- two fresh private replays with byte-identical inventories; and
- out-of-band attachment of the frozen per-reference attribution record IDs.

The future runner must preserve one worker and at least 15 GiB free disk,
reverify the private source inventory before conversion, use journaled atomic
partial-then-rename writes, and redact private paths and per-reference hashes
from public evidence.

## Explicitly closed scope

The same record must leave all of the following false:

- ODAQ processed-condition or listening-score access;
- perceptual-metric execution;
- stimulus generation or degradation-rating collection;
- listener-response collection;
- sealed-evidence access;
- independent-transfer or final-validation claims; and
- any public degradation verdict.

Projection copies remain one-provider development plumbing. They are not codec
conditions, perceptual truth, metric evidence, or listening results.

## Gate

The committed execution plan intentionally contains no authorization file and
sets every live retained-audio authority to false. Its validator tests the
future record shape only with an in-memory synthetic object. That object is not
written to disk and cannot be used as evidence of a responsible-human choice.

The next permissible action is to record the responsible human's explicit
successor authorization. Only after that record is committed and exact-head CI
passes may a separate private live runner be implemented and exercised.

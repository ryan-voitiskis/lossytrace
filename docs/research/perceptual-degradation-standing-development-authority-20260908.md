# Standing authority for bounded digital development — 2026-09-08

The user has authorized as many further development attempts as needed. The
[standing authorization](../../benchmarks/perceptual-degradation-v1/digital-development-standing-authorization-20260908.json)
removes the need to ask again for each technical canary. It does not reopen a
consumed run or convert failed observations into successes.

Every batch still has a prospectively frozen question, implementation, case
inventory, resource limits and stopping rules. Failures and unsupported cases
remain visible. Synthetic full-size observations require a clean local
pre-observation commit. Each real-reference/codec batch additionally requires
its own committed hash-bound scope and successful exact-execution-head CI.
Use one audio worker, preserve the 15 GiB output-volume reserve and keep derived
audio temporary. No automatic repair, replacement or deadline change within a
batch is implied. Later development batches may investigate a recorded failure
under new prospective rules without a fresh user permission question.

The first successor extends native correlation to the global refinement stage,
with exact candidate-by-candidate equivalence and unchanged scientific limits.
It does not replace the alignment model with metadata-driven correction or
assume the unprofiled real canary's dominant hotspot. Broader full-size synthetic
workloads must test cost and runtime margin before another real canary is frozen.

This is technical development only. No capture, sound-card change, playback,
perceptual metric, human collection, source-trait assignment, no-reference
training, sealed/provider-score access, outreach, spending, merge, release or
public verdict is authorized by this standing permission. The overall research
objective and its human-calibration/independent-transfer gates are unchanged.
